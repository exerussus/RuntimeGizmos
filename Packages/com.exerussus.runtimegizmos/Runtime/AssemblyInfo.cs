using System.Runtime.CompilerServices;

// Тестовым сборкам нужен доступ к внутренностям: рендереру, каналам батчинга,
// нативным буферам, разметке вершин. Проверять их через публичный API нельзя —
// он про рисование, а не про то, что происходит с буферами внутри.
//
// Ссылка на несуществующую сборку безвредна: в проектах, куда тестовый пакет
// не поставлен, атрибут просто ни на что не указывает.
[assembly: InternalsVisibleTo("RuntimeGizmos.Tests.Editor")]
[assembly: InternalsVisibleTo("RuntimeGizmos.Tests.Runtime")]
